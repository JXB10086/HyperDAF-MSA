# HyperDAF-MSA 依赖清单 (阶段1-3 实测可用版本)
# 说明:
#   - torch 为 CPU 版; 若从干净 venv 安装请用官方 CPU 索引:
#       pip install torch --index-url https://download.pytorch.org/whl/cpu
#   - scikit-learn 仅用于 t-SNE 表征诊断 (可选)。当前镜像对 Python UA 返回 403
#     无法安装, 诊断脚本已改用 numpy 自实现精确 t-SNE, 不依赖 sklearn。
#     如后续网络恢复可取消注释: scikit-learn==1.3.2
torch==2.4.1+cpu
numpy==1.24.4
pandas==2.0.3
matplotlib==3.7.5
scipy==1.10.1
# scikit-learn==1.3.2   # 可选, 当前不可用(镜像403), 诊断已自实现 t-SNE
